import React from 'react'

const EmptySectionMessage = ({ message }) => {
    return (
        <div className="w-72 h-40 flex justify-center items-center text-violet-700 font-main">{message}</div>
    )
}

export default EmptySectionMessage